export class Constants {
  
  public static PROPS = {
    APP_PORT                : 'application.port',
    DB_DATA_SOURCE_URL      : 'db.data.source.url',
    DB_TEMP_QUEUE_SIZE      : 'db.temporary.queue.size',
    DB_CONNECTION_TIMEOUT_MS: 'db.connection.timeout.ms',
    DB_CONNECTION_POOL_SIZE : 'db.connection.pool.size',
    DB_RECONNECT_INTERVAL_MS: 'db.reconnect.interval.ms',
    DB_RECONNECT_RETRY_COUNT: 'db.reconnect.retry.count',
    DB_SOCKET_TIMEOUT_MS    : 'db.socket.timeout.ms',
    ELASTIC_SEARCH_URL      : 'elastic.search.url',
  };
  
}